[common]
    bin = "ls"
    refBin = "ls"

[[Test]]
    name = "Help"
    desc = "ls's help"
    args = ["-h"]
    refArgs = ["-h"]

    [Test.expected]
        status = 0

[[Test]]
    name = "others"
    desc = "Will display CBM's help." # Optional
    args = ["-h"] # Optional
    refArgs = ["-h"] # Optional

    [Test.expected]
        status = 0

